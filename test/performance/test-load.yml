# tests/performance/test-load.yml
config:
  target: 'https://sistemadeventa.onrender.com'
  tls:
    rejectUnauthorized: false
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - flow:
      - get:
          url: "/usuarios"

<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>Detalle de Reembolso</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- importante para móviles -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-light">
    <%- include ../partials/navbar.ejs %>

        <div class="container my-5">
            <h2 class="mb-4 text-center text-danger">Reembolso de Pedido Cancelado</h2>

            <!-- Card centrada y con ancho máximo -->
            <div class="card mx-auto p-4 shadow-sm border-0" style="max-width: 700px;">
                <!-- Productos -->
                <h5 class="card-title mb-3">🛍️ Productos cancelados</h5>
                <% productos.forEach(p=> { %>
                    <!-- Ajuste responsivo: imagen y texto se apilan en móviles -->
                    <div class="d-flex flex-column flex-sm-row align-items-start align-items-sm-center mb-3">
                        <img src="<%= p.imagen_url %>" alt="Producto" class="rounded mb-2 mb-sm-0 me-sm-3"
                            style="width: 100%; max-width: 80px; height: auto;">
                        <div>
                            <strong>
                                <%= p.nombre_producto %>
                            </strong><br>
                            Cantidad: <%= p.cantidad %> | Precio unitario: $<%= p.precio_unitario %><br>
                                    Subtotal: $<%= p.subtotal %>
                        </div>
                    </div>
                    <% }) %>

                        <hr>

                        <!-- Envío -->
                        <h5 class="card-title mb-3">🚚 Información de envío</h5>
                        <p class="mb-2">
                            <strong>Dirección:</strong>
                            <%= pedido.direccion %>, <%= pedido.municipio %>, <%= pedido.estado2 %><br>
                                        <strong>Ciudad:</strong>
                                        <%= pedido.ciudad %><br>
                                            <strong>Código Postal:</strong>
                                            <%= pedido.codigo_postal %><br>
                                                <strong>Teléfono:</strong>
                                                <%= pedido.telefono %><br>
                                                    <strong>Número de seguimiento:</strong>
                                                    <%= pedido.numero_seguimiento || 'No asignado' %><br>
                                                        <strong>Fecha de cancelación:</strong>
                                                        <%= pedido.fecha_cancelacion ? new
                                                            Date(pedido.fecha_cancelacion).toLocaleDateString('es-MX')
                                                            : 'No disponible' %>
                        </p>

                        <hr>

                        <!-- Reembolso -->
                        <h5 class="card-title mb-3">💸 Detalles de reembolso</h5>
                        <p>
                            <strong>Método de pago:</strong>
                            <%= pedido.metodo_pago || 'No especificado' %><br>
                                <strong>Monto a reembolsar:</strong> $<%= Number(pedido.total || 0).toFixed(2) %><br>
                                    <strong>Estado actual:</strong>
                                    <span class="badge bg-danger text-uppercase">Cancelado</span><br>
                                    <strong>Tiempo estimado de reembolso:</strong> 3 a 5 días hábiles
                        </p>

                        <p class="text-success mt-3">
                            Gracias por tu paciencia. Si tienes dudas, contáctanos desde el formulario de ayuda.
                        </p>
            </div>
        </div>

        <%- include ../partials/footer.ejs %>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>